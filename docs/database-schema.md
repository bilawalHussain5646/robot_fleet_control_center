# Database Schema

Core tables: users, robots, robot_commands, robot_telemetry, tasks, alerts, maintenance_records, audit_logs, refresh_tokens. The acceptance-list tables for permissions, camera sources, settings, and AI conversations are documented extension points for the next migration pass.
