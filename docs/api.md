# API

Base path: `/api/v1`.

Routers include auth, robots, commands, telemetry, tasks, alerts, maintenance, analytics, audit logs, assistant, and health. OpenAPI documentation is available at `/docs`.
