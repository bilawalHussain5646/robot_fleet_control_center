# Deployment

Use Docker Compose or a container orchestrator. Provide managed PostgreSQL and Redis, HTTPS, secure JWT secret storage, restricted CORS, log aggregation, backups, and health checks. Nginx proxies frontend, API, and WebSocket traffic.
